# Application Environment (development | preview | production)
# This will be overridden by EAS build profiles
EXPO_PUBLIC_APP_ENV=development

# API Configuration
EXPO_PUBLIC_API_URL=https://dummyjson.com/

# Supabase Configuration
# IMPORTANT: For production builds, these values MUST be stored in EAS Secrets
# Do NOT commit real API keys to version control
# Use: eas secret:create --name EXPO_PUBLIC_SUPABASE_URL --value <your-url>
# Use: eas secret:create --name EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY --value <your-key>
EXPO_PUBLIC_SUPABASE_URL=your-supabase-url-here
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-supabase-publishable-key-here

# Google Places API
# IMPORTANT: For production builds, store in EAS Secrets
# Use: eas secret:create --name EXPO_PUBLIC_GOOGLE_PLACES_API_KEY --value <your-key>
EXPO_PUBLIC_GOOGLE_PLACES_API_KEY=your-google-places-api-key-here

# For local development, create a .env.local file with your actual values
# .env.local is in .gitignore and will not be committed

# EAS Build Secrets Setup Instructions:
# 1. Regenerate your Google Places API key (previous key was exposed in git history)
# 2. Run the following commands to store secrets in EAS:
#    eas secret:create --name EXPO_PUBLIC_SUPABASE_URL --value <url>
#    eas secret:create --name EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY --value <key>
#    eas secret:create --name EXPO_PUBLIC_GOOGLE_PLACES_API_KEY --value <key>
# 3. EAS will automatically inject these values during builds
